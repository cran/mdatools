% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ddsimca3w.R
\name{ddsimca.parafac}
\alias{ddsimca.parafac}
\title{DD-SIMCA with PARAFAC decomposition for 3-way data}
\usage{
ddsimca.parafac(
  x,
  classname,
  dim = NULL,
  ncomp = min(dim[1], dim[2], nrow(x) - 1),
  non.negative = FALSE,
  nf.as.sum = FALSE,
  alpha = 0.05,
  gamma = 0.01,
  verbose = FALSE
)
}
\arguments{
\item{x}{either an n x J x K numeric array, or an already-unfolded numeric
matrix (n x (J*K)). A matrix is interpreted column-major (R's native order,
as produced by \code{dim(x) <- c(n, J*K)}) and refolded through \code{dim}.
When an array is supplied \code{dim} is inferred.}

\item{classname}{name of the target class (<= 20 chars).}

\item{dim}{integer vector c(J, K), both >= 2. Optional (inferred) when \code{x}
is a 3-way array; required when \code{x} is a matrix.}

\item{ncomp}{number of PARAFAC components (<= min(J, K, n-1)).}

\item{non.negative}{logical, use non-negative PARAFAC.}

\item{nf.as.sum}{logical, full-distance DoF rule (FALSE = data-driven eq.7).}

\item{alpha}{significance level for extremes.}

\item{gamma}{significance level for outliers.}

\item{verbose}{logical, if \code{TRUE} report sub-models whose CP-ALS reached
the iteration limit without converging (usually a sign of more components
than the data supports). Silent by default.}
}
\value{
model of class \code{c("ddsimca.parafac","ddsimca3w","ddsimca")}.
}
\description{
DD-SIMCA with PARAFAC decomposition for 3-way data
}
